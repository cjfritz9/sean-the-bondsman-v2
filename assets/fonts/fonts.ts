import { Inter } from 'next/font/google';
import { Kanit } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const kanit = Kanit({ weight: '500', subsets: ['latin'], variable: '--font-kanit' })

